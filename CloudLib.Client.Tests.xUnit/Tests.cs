using System;

using CloudLib.Client.ViewModels;

using Xunit;

namespace CloudLib.Client.Tests.XUnit
{
    // TODO WTS: Add appropriate tests
    public class Tests
    {
        [Fact]
        public void TestMethod1()
        {
        }

        // TODO WTS: Add tests for functionality you add to ContentGridViewModel.
        [Fact]
        public void TestContentGridViewModelCreation()
        {
            // This test is trivial. Add your own tests for the logic you add to the ViewModel.
            var vm = new ContentGridViewModel();
            Assert.NotNull(vm);
        }

        // TODO WTS: Add tests for functionality you add to ImageGalleryViewModel.
        [Fact]
        public void TestImageGalleryViewModelCreation()
        {
            // This test is trivial. Add your own tests for the logic you add to the ViewModel.
            var vm = new ImageGalleryViewModel();
            Assert.NotNull(vm);
        }

        // TODO WTS: Add tests for functionality you add to InkDrawPictureViewModel.
        [Fact]
        public void TestInkDrawPictureViewModelCreation()
        {
            // This test is trivial. Add your own tests for the logic you add to the ViewModel.
            var vm = new InkDrawPictureViewModel();
            Assert.NotNull(vm);
        }

        // TODO WTS: Add tests for functionality you add to InkSmartCanvasViewModel.
        [Fact]
        public void TestInkSmartCanvasViewModelCreation()
        {
            // This test is trivial. Add your own tests for the logic you add to the ViewModel.
            var vm = new InkSmartCanvasViewModel();
            Assert.NotNull(vm);
        }

        // TODO WTS: Add tests for functionality you add to ListDetailsViewModel.
        [Fact]
        public void TestListDetailsViewModelCreation()
        {
            // This test is trivial. Add your own tests for the logic you add to the ViewModel.
            var vm = new ListDetailsViewModel();
            Assert.NotNull(vm);
        }

        // TODO WTS: Add tests for functionality you add to MainViewModel.
        [Fact]
        public void TestMainViewModelCreation()
        {
            // This test is trivial. Add your own tests for the logic you add to the ViewModel.
            var vm = new MainViewModel();
            Assert.NotNull(vm);
        }

        // TODO WTS: Add tests for functionality you add to MediaPlayerViewModel.
        [Fact]
        public void TestMediaPlayerViewModelCreation()
        {
            // This test is trivial. Add your own tests for the logic you add to the ViewModel.
            var vm = new MediaPlayerViewModel();
            Assert.NotNull(vm);
        }

        // TODO WTS: Add tests for functionality you add to SettingsViewModel.
        [Fact]
        public void TestSettingsViewModelCreation()
        {
            // This test is trivial. Add your own tests for the logic you add to the ViewModel.
            var vm = new SettingsViewModel();
            Assert.NotNull(vm);
        }

        // TODO WTS: Add tests for functionality you add to TabViewViewModel.
        [Fact]
        public void TestTabViewViewModelCreation()
        {
            // This test is trivial. Add your own tests for the logic you add to the ViewModel.
            var vm = new TabViewViewModel();
            Assert.NotNull(vm);
        }

        // TODO WTS: Add tests for functionality you add to TelerikDataGridViewModel.
        [Fact]
        public void TestTelerikDataGridViewModelCreation()
        {
            // This test is trivial. Add your own tests for the logic you add to the ViewModel.
            var vm = new TelerikDataGridViewModel();
            Assert.NotNull(vm);
        }

        // TODO WTS: Add tests for functionality you add to TreeViewViewModel.
        [Fact]
        public void TestTreeViewViewModelCreation()
        {
            // This test is trivial. Add your own tests for the logic you add to the ViewModel.
            var vm = new TreeViewViewModel();
            Assert.NotNull(vm);
        }
    }
}
